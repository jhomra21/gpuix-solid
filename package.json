{
  "name": "gpuix-solid-workspace",
  "private": true,
  "version": "0.0.0",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --filter '@jhomra21/gpuix-solid' build && bun run --filter 'gpuix-solid-counter' build",
    "typecheck": "bun run --filter '@jhomra21/gpuix-solid' typecheck && bun run --filter 'gpuix-solid-counter' typecheck",
    "test": "bun run --filter '@jhomra21/gpuix-solid' test",
    "lint": "oxlint .",
    "release:check": "bun run lint && bun run typecheck && bun run test && bun run build",
    "release:test": "node --test scripts/release.test.mjs",
    "example:counter": "bun run --filter '@jhomra21/gpuix-solid' build && bun run --filter 'gpuix-solid-counter' run:counter",
    "example:native-text": "bun run --filter '@jhomra21/gpuix-solid' build && bun run --filter 'gpuix-solid-counter' run:native-text"
  },
  "devDependencies": {
    "@oxlint/plugins": "1.79.0",
    "oxlint": "1.79.0"
  }
}
