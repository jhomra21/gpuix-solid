{
  "name": "gpuix-solid-site",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "bun run build.mjs",
    "dev": "bun run build && bunx wrangler@4.134.0 dev",
    "deploy": "bun run build && bunx wrangler@4.134.0 deploy",
    "preview:upload": "bun run build && bunx wrangler@4.134.0 versions upload"
  }
}
