{
  "name": "gpuix-solid1-diffusion-source",
  "private": true,
  "type": "module",
  "scripts": {
    "host:build": "cd ../../packages/solid1 && bun install --no-save && bun run build",
    "source:prepare": "node scripts/prepare-source.mjs",
    "build": "bun run host:build && bun run source:prepare && bun run generate:styles && vite build",
    "build:test": "bun run host:build && bun run source:prepare && bun run generate:styles && vite build --config vite.test.config.ts",
    "test:native": "bun run build:test && node scripts/run-test.mjs",
    "start": "bun dist/app/index.js",
    "run": "bun run build && bun run start",
    "build:test:edge": "cd ../../packages/solid1 && bun run build && cd ../../examples/solid1-diffusion && bun run source:prepare && bun run generate:styles && vite build --config vite.test.config.ts",
    "test:native:edge": "bun run build:test:edge && node scripts/run-test.mjs",
    "generate:styles": "node scripts/generate-native-tailwind.mjs"
  },
  "dependencies": {
    "@gpuix/native": "0.9.0",
    "@jhomra21/gpuix-solid1": "file:../../packages/solid1",
    "fake-indexeddb": "6.2.5",
    "koota": "0.6.6",
    "solid-js": "1.9.15"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.9.2",
    "vite": "^8.1.5",
    "vite-plugin-solid": "^2.11.14"
  }
}
